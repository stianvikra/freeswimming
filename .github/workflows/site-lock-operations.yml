name: Site Lock Operations

on:
  workflow_dispatch:
    inputs:
      action:
        description: "Operation to run"
        required: true
        type: choice
        options:
          - lock_on
          - lock_off
      target_environment:
        description: "Target Vercel environment"
        required: true
        type: choice
        options:
          - preview
          - production
      reason:
        description: "Reason for operation (audit trail)"
        required: true
        default: "Manual site-lock operation"
        type: string
      confirm:
        description: "Type APPLY to confirm"
        required: true
        type: string

permissions:
  contents: read
  actions: read

concurrency:
  group: site-lock-ops-${{ inputs.target_environment }}
  cancel-in-progress: false

jobs:
  operate-site-lock:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment:
      name: ${{ inputs.target_environment == 'production' && 'site-lock-production' || 'site-lock-preview' }}
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      OPERATION: ${{ inputs.action }}
      TARGET_ENVIRONMENT: ${{ inputs.target_environment }}
      OPERATION_REASON: ${{ inputs.reason }}
      OPERATION_CONFIRM: ${{ inputs.confirm }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Validate operation request
        id: validate
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/site-lock-ops

          if [ "$OPERATION_CONFIRM" != "APPLY" ]; then
            echo "::error::Input 'confirm' must be exactly APPLY."
            exit 1
          fi

          case "$OPERATION" in
            lock_on)
              desired="1"
              rollback="lock_off"
              ;;
            lock_off)
              desired="0"
              rollback="lock_on"
              ;;
            *)
              echo "::error::Unsupported operation: $OPERATION"
              exit 1
              ;;
          esac

          case "$TARGET_ENVIRONMENT" in
            preview|production) ;;
            *)
              echo "::error::Unsupported target environment: $TARGET_ENVIRONMENT"
              exit 1
              ;;
          esac

          target_git_branch="${GITHUB_REF_NAME:-main}"

          echo "desired=$desired" >> "$GITHUB_OUTPUT"
          echo "rollback=$rollback" >> "$GITHUB_OUTPUT"
          echo "target_git_branch=$target_git_branch" >> "$GITHUB_OUTPUT"

      - name: Check required Vercel secrets
        shell: bash
        run: |
          set -euo pipefail
          missing=""
          for key in VERCEL_TOKEN VERCEL_ORG_ID VERCEL_PROJECT_ID; do
            if [ -z "${!key:-}" ]; then
              if [ -n "$missing" ]; then
                missing="$missing, "
              fi
              missing="${missing}${key}"
            fi
          done

          if [ -n "$missing" ]; then
            echo "::error::Missing required Vercel secrets: $missing"
            exit 1
          fi

      - name: Enforce production reviewer gate
        if: inputs.target_environment == 'production'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail

          api_url="${{ github.api_url }}/repos/${{ github.repository }}/environments/site-lock-production"

          if ! response="$(curl -fsS \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            "$api_url")"; then
            echo "::error::Could not read GitHub environment settings for site-lock-production."
            echo "::error::Ensure environment exists and this workflow has permission to read protection rules."
            exit 1
          fi

          reviewer_count="$(
            printf '%s' "$response" | node -e 'const fs=require("fs"); const data=JSON.parse(fs.readFileSync(0,"utf8")); const rules=Array.isArray(data.protection_rules)?data.protection_rules:[]; const required=rules.find((rule)=>rule && rule.type==="required_reviewers"); const count=required && Array.isArray(required.reviewers)?required.reviewers.length:0; process.stdout.write(String(count));'
          )"

          if [ "$reviewer_count" -lt 1 ]; then
            echo "::error::site-lock-production must require at least one reviewer before production operations can run."
            exit 1
          fi

      - name: Install Vercel CLI
        run: npm i -g vercel@latest

      - name: Pull Vercel project settings
        shell: bash
        run: |
          set -euo pipefail
          vercel pull --yes --environment="$TARGET_ENVIRONMENT" --token="$VERCEL_TOKEN"

      - name: Validate lock prerequisites for lock_on
        if: inputs.action == 'lock_on'
        shell: bash
        run: |
          set -euo pipefail
          env_list="$(vercel env ls "$TARGET_ENVIRONMENT" --token="$VERCEL_TOKEN")"

          missing=""
          for key in SITE_LOCK_PASSWORD_HASH SITE_LOCK_BYPASS_TOKEN; do
            if ! printf '%s\n' "$env_list" | grep -q "$key"; then
              if [ -n "$missing" ]; then
                missing="$missing, "
              fi
              missing="${missing}${key}"
            fi
          done

          if [ -n "$missing" ]; then
            echo "::error::Cannot run lock_on. Missing required site-lock vars in Vercel $TARGET_ENVIRONMENT: $missing"
            exit 1
          fi

      - name: Apply SITE_LOCK_ENABLED in Vercel environment
        shell: bash
        run: |
          set -euo pipefail
          desired="${{ steps.validate.outputs.desired }}"
          target_git_branch="${{ steps.validate.outputs.target_git_branch }}"

          if [ "$TARGET_ENVIRONMENT" = "preview" ]; then
            # Explicit branch scoping avoids interactive prompts in CI and matches deployed preview ref.
            vercel env rm SITE_LOCK_ENABLED preview "$target_git_branch" --yes --token="$VERCEL_TOKEN" || true
            vercel env rm SITE_LOCK_ENABLED preview --yes --token="$VERCEL_TOKEN" || true
            printf '%s\n' "$desired" | vercel env add SITE_LOCK_ENABLED preview "$target_git_branch" --token="$VERCEL_TOKEN"
            exit 0
          fi

          env_list="$(vercel env ls "$TARGET_ENVIRONMENT" --token="$VERCEL_TOKEN" || true)"
          if printf '%s\n' "$env_list" | grep -q "SITE_LOCK_ENABLED"; then
            vercel env rm SITE_LOCK_ENABLED "$TARGET_ENVIRONMENT" --yes --token="$VERCEL_TOKEN"
          fi

          printf '%s\n' "$desired" | vercel env add SITE_LOCK_ENABLED "$TARGET_ENVIRONMENT" --token="$VERCEL_TOKEN"

      - name: Deploy updated environment
        id: deploy
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/site-lock-ops
          deploy_output_file="artifacts/site-lock-ops/deploy-output.log"

          if [ "$TARGET_ENVIRONMENT" = "preview" ]; then
            vercel deploy --target=preview --yes --token="$VERCEL_TOKEN" >"$deploy_output_file" 2>&1
          else
            vercel deploy --prod --yes --token="$VERCEL_TOKEN" >"$deploy_output_file" 2>&1
          fi

          deploy_url="$(grep -Eo 'https://[^ ]+' "$deploy_output_file" | tail -n 1 || true)"
          if [ -z "$deploy_url" ]; then
            echo "::error::Could not extract deployment URL from Vercel output."
            cat "$deploy_output_file"
            exit 1
          fi

          echo "deploy_url=$deploy_url" >> "$GITHUB_OUTPUT"
          echo "Deploy URL: $deploy_url"

      - name: Smoke verify lock behavior
        id: smoke
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/site-lock-ops
          home_headers_file="artifacts/site-lock-ops/smoke-home-headers.txt"
          api_headers_file="artifacts/site-lock-ops/smoke-api-headers.txt"
          deploy_url="${{ steps.deploy.outputs.deploy_url }}"

          curl -sS -I --max-time 20 "$deploy_url" > "$home_headers_file"
          curl -sS -I --max-time 20 "$deploy_url/api/runtime/flags" > "$api_headers_file"

          home_status_code="$(awk 'NR==1 {print $2}' "$home_headers_file")"
          home_location_header="$(awk 'tolower($1)=="location:" {print $2}' "$home_headers_file" | tr -d '\r' | tail -n 1)"
          api_status_code="$(awk 'NR==1 {print $2}' "$api_headers_file")"
          api_location_header="$(awk 'tolower($1)=="location:" {print $2}' "$api_headers_file" | tr -d '\r' | tail -n 1)"

          if [ "$OPERATION" = "lock_on" ]; then
            if [ "$api_status_code" != "423" ]; then
              echo "::error::lock_on smoke check failed. Expected /api/runtime/flags to return 423."
              cat "$api_headers_file"
              exit 1
            fi

            if [[ "$home_location_header" != *"/preview-access"* ]]; then
              echo "::warning::lock_on home route did not return redirect header; API lock status validated via 423."
            fi
          else
            if [ "$api_status_code" = "423" ]; then
              echo "::error::lock_off smoke check failed. /api/runtime/flags still returns 423."
              cat "$api_headers_file"
              exit 1
            fi

            if [[ "$home_location_header" == *"/preview-access"* ]]; then
              echo "::error::lock_off smoke check failed. Unexpected redirect to /preview-access."
              cat "$home_headers_file"
              exit 1
            fi
          fi

          echo "home_status_code=$home_status_code" >> "$GITHUB_OUTPUT"
          echo "home_location_header=$home_location_header" >> "$GITHUB_OUTPUT"
          echo "api_status_code=$api_status_code" >> "$GITHUB_OUTPUT"
          echo "api_location_header=$api_location_header" >> "$GITHUB_OUTPUT"

      - name: Write operation summary
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/site-lock-ops
          summary_file="artifacts/site-lock-ops/summary.md"
          run_url="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          cat > "$summary_file" <<EOF
          # Site lock operation summary

          - actor: \`${{ github.actor }}\`
          - operation: \`$OPERATION\`
          - target environment: \`$TARGET_ENVIRONMENT\`
          - desired SITE_LOCK_ENABLED: \`${{ steps.validate.outputs.desired }}\`
          - rollback action: \`${{ steps.validate.outputs.rollback }}\`
          - reason: $OPERATION_REASON
          - deployment URL: ${{ steps.deploy.outputs.deploy_url }}
          - smoke home status code: \`${{ steps.smoke.outputs.home_status_code }}\`
          - smoke home location header: \`${{ steps.smoke.outputs.home_location_header }}\`
          - smoke api status code (`/api/runtime/flags`): \`${{ steps.smoke.outputs.api_status_code }}\`
          - smoke api location header: \`${{ steps.smoke.outputs.api_location_header }}\`
          - run URL: $run_url
          EOF

          cat "$summary_file" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload site-lock operation artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: site-lock-operation-${{ inputs.target_environment }}-${{ inputs.action }}
          path: |
            artifacts/site-lock-ops/summary.md
            artifacts/site-lock-ops/smoke-home-headers.txt
            artifacts/site-lock-ops/smoke-api-headers.txt
            artifacts/site-lock-ops/deploy-output.log
          if-no-files-found: ignore
          retention-days: 14
